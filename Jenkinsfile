pipeline {
    agent any
    stages {
        stage("Build")
        {
            steps
            {
                script {
                            echo "INFO: Deploy Stage"
                            sh "docker ps"
                    }
            }
        }
    }
}
